# 《Loop Engineer：让 AI 在工程闭环里自动推进任务》视频脚本

## 视频定位

- 类型：无真人出镜的科技知识解说
- 建议时长：1.1 倍速下约 6 分钟（偏快、无大段停顿）
- 画幅：16:9（可二次裁切为 9:16）
- 目标观众：开发者、技术管理者、AI Agent/自动化从业者
- 核心观点：AI 真正进入工程，不是因为它更会“回答”，而是因为它能在有目标、有上下文、有工具、有验证、有边界的闭环里持续推进任务。
- 叙事结构：故事钩子 → 概念反差 → 闭环机制 → Loop Contract → 多 Loop 案例 → 适用场景 → 角色变化
- 视觉基调：深色工程控制台、荧光绿/蓝状态线、流程节点与日志卡片；避免纯 PPT 罗列。

## 锁定旁白 V2（高结构密度版）

> 以下旁白可以脱离画面单独成立。全片依次介绍 Loop Engineer 的定义、运行结构和落地方式。

这是一个 Loop Engineer DEMO。

场景很简单：凌晨三点，线上出现一个阻塞性 Bug。用户无法操作，客服收到投诉，监控开始报警。

过去，接下来的流程是：叫醒工程师、查日志、找代码、改 Bug、跑测试、再发版。

在这个 DEMO 里，这套流程先交给 AI 执行。

现在画面里，AI 正在读取错误，检查日志，再定位相关代码。找到原因以后，它会尝试修复并运行测试。测试失败，它就读取新的失败信息，继续修改，再次验证。直到测试通过，或者确认信息不足，请求人类介入。

这就是 Loop Engineer。

接下来分三部分展开：先介绍它和普通 AI 编程的区别，再拆解一个 Loop 的运行结构，最后看它如何在真实工程里安全落地。

先从 Loop Engineer 的定义开始。

普通 AI 编程通常停在生成答案。你让 AI 解释报错、生成函数或者写一个组件。它给出答案，任务就结束了。至于答案是否有效，仍然要由人验证和修正。

刚才这个 DEMO 的不同之处，是 AI 没有停在第一次回答。它的目标不是生成一次答案，而是持续推进一个结果。

它会重复一套固定动作：观察当前状态，读取上下文，决定下一步，执行动作，获得反馈，再根据反馈调整。通过验证，就交付结果；没有通过，就进入下一轮。

所以，两者最核心的区别可以压缩成一句话：普通 AI 重在生成，Loop Engineer 重在闭环。

定义清楚以后，再看一个 Loop 的运行结构。

可以把它拆成六个部分。

第一，Trigger，也就是触发器。它决定任务什么时候开始。可能是监控报警、定时任务，也可能是一条被标记为 Bug 的工单。

第二，Context，也就是上下文。代码、日志、文档、用户反馈、历史提交和测试结果，都属于上下文。没有上下文，AI 只能猜；上下文完整，AI 才可能定位真正原因。

第三，Tools，也就是工具。AI 必须能够搜索代码、读取文件、修改内容、运行命令和查看结果。只会分析的 AI 是顾问，能够执行动作的 AI 才可能成为工程代理。

第四，Verify，也就是验证。单元测试、类型检查、接口检查和页面截图，都可以成为验证标准。没有验证，AI 就不知道自己的修改究竟有没有用，也就无法形成闭环。

第五，Logs，也就是运行记录。每一轮看到了什么、做了什么、失败在哪里、下一步是什么，都要被记录。这样，自动运行才不是黑箱。

第六，Output，也就是最终交付。它可以是一份修复报告、一个代码改动，或者一个等待审核的 PR。

这六个部分还需要一个共同的约束层：Loop Contract，也就是任务合同。

任务合同要写清楚目标、输入、流程、权限、验证标准、停止条件和最终产物。

比如，AI 可以读取日志，可以修改指定模块，可以运行测试，也可以生成 PR；但它不能读取敏感密钥，不能修改范围外的代码，更不能直接部署生产。

所以 Loop Contract 的本质，不是让 AI 自由发挥，而是让它在任务合同的边界内持续推进。

到这里，Loop 的定义和运行结构已经清楚了。下面进入真实工程的落地方式。

真实系统通常不会造一个无所不能的超级 Agent，而是把复杂任务拆成多个职责清晰的小 Loop。

仍然以半夜修 Bug 为例。完整流程可以拆成六段。

第一段，sync，同步客服工单、用户反馈和监控异常。

第二段，feedback，把零散反馈归类，提炼成明确的问题信号。

第三段，task，判断信号是否达到处理阈值，并生成一条待批准任务。

第四段，coding，进入代码仓库定位并修复问题。

第五段，verify，对照验收标准运行测试，生成验证报告。

第六段，followup，根据修复结果生成用户回访草稿。

这条流水线中，有两个门禁尤其重要。

第一个门禁在 coding 之前。只有任务状态从 proposed 变成 approved，AI 才能修改代码。

第二个门禁在 followup 之后。AI 可以生成回访草稿，但绝不自动发送。

保留这两个人工节点，是因为发现问题、修改代码、部署生产和联系用户，承担的风险完全不同。好的 Loop 不是删除所有人，而是把人放在真正需要判断、授权和负责的位置。

现在回头看，Loop Engineer 的价值已经很清楚了。

它不是让 AI 在生产环境里自由发挥，而是把复杂任务拆成多个可观察、可审计、可批准、可验证的小循环。

而且，这套结构并不只适用于修 Bug。客服可以循环处理工单并沉淀产品信号；SEO 可以持续发现关键词机会并监控结果；产品团队可以读取行为数据、提出实验，再生成等待审核的小 PR。

场景虽然不同，底层结构始终相同：触发任务，读取上下文，调用工具，执行动作，验证结果，记录过程，并在边界内进入下一轮。

最后总结一下。

Loop Engineer 不是更复杂的 Prompt，也不只是更强的 AI 编程助手。它是一种新的工程组织方式：让 AI 在一个有目标、有上下文、有工具、有验证、有记录、有边界的循环里持续工作。

工程师的角色也会随之变化：从亲自盯住每一步，转向定义目标、设计合同、配置工具、设置验证、控制权限和审核结果。

从 Prompt 到 Loop，真正发生的变化，是 AI 不再只负责回答问题，而是开始持续推进结果。

---

## 分镜脚本

### 01｜凌晨三点，线上崩了（0:00–0:20）

**画面**

主画面用简洁的时间、报警和标题包装建立场景；右下或右侧保留一个持续存在的子画面，播放你录制的真实 DEMO。DEMO 从错误出现、AI 接收任务开始，保留鼠标移动、日志滚动和终端执行过程。

子画面建议占画面宽度的 35%–45%，不要频繁缩放。关键操作发生时，子画面短暂放大到主画面；解释结束后再退回角落。

**旁白**

这是我做的一个 Loop Engineer DEMO。

场景很简单：凌晨三点，线上出现一个阻塞性 Bug。过去，工程师要被叫醒，再手动查日志、找代码、修改和测试。

在这个 DEMO 里，这套排查流程先交给 AI 执行。

**屏幕文字**

`Loop Engineer 自动修复阻塞性 Bug`

`03:00 AM / Production Incident`

**音效**

低频报警声；终端启动声。

---

### 02｜它不是回答，而是在推进（0:20–0:43）

**画面**

以实录 DEMO 为主画面，按照真实操作顺序展示：读取错误 → 查看日志 → 搜索代码 → 修改文件 → 运行测试。剪掉等待和重复输入，只保留每一步的结果。测试失败时停留 1 秒，再展示 AI 读取失败信息并继续修改。

画面左上角同步出现当前阶段标签：`OBSERVE`、`LOCATE`、`FIX`、`VERIFY`、`RETRY`。

**旁白**

现在画面里，AI 正在读取错误信息，检查日志，再定位相关代码。

找到原因以后，它会修改代码并运行测试。测试失败，就读取新的失败信息，继续调整，再次验证。

这个持续推进任务的过程，就是 Loop Engineer。

**屏幕文字**

`Observe → Act → Verify → Adjust`

---

### 03｜从 Prompt 到 Loop（0:43–1:07）

**画面**

DEMO 子画面继续保留，并定格在测试通过或生成修复结果的状态。主画面用一条极简对比动画解释：左侧“输入 Prompt → 输出答案”，右侧“观察 → 执行 → 验证 → 调整 → 结果”。

**旁白**

普通的 AI 编程，通常停在生成答案。代码是否有效，仍然要由人继续验证和修正。

刚才这个 DEMO 的不同之处，是 AI 没有停在第一次回答。它进入了真实工程环境，根据每一次执行结果继续行动，直到产出可验证的修复。

普通 AI 重在生成，Loop Engineer 重在闭环。

**屏幕文字**

左：`Prompt = 生成答案`

右：`Loop = 推进结果`

---

### 04｜一个 Loop 的六个部件（1:07–1:38）

**画面**

中心形成一个循环，六个节点逐一亮起：Trigger、Context、Tools、Verify、Logs、Output。最外层再出现一圈半透明边界：Loop Contract。

**旁白**

一个可用的 Loop，至少要有六个部件。

Trigger，决定什么时候启动。Context，提供代码、日志、文档和历史记录。Tools，让 AI 能搜索、修改和运行。Verify，用测试和检查判断动作是否有效。Logs，记录每一轮发生了什么。最后，是可以验收的 Output。

但真正把这些部件约束起来的，是最外层的 Loop Contract。

**屏幕文字**

`Trigger / Context / Tools / Verify / Logs / Output`

`Loop Contract = 约束层`

---

### 05｜Loop Contract：不是“你看着办”（1:38–2:08）

**画面**

一份 `loop-contract.md` 在屏幕中央展开。Goal、Inputs、Workflow、Permissions、Verification、Stop Conditions、Output 依次高亮。随后出现危险操作“Deploy Production”，被权限边界拦截。

**旁白**

Loop Contract 不是一句模糊的 Prompt，而是一份任务合同。

它写清目标是什么，可以读取哪些输入，按什么流程推进；允许改哪里，禁止做什么；怎样才算完成，什么情况下必须停止，以及最终交付什么。

所以它不是“你自己看着办”，而是“在这份合同内循环推进”。自动化可以很强，但边界必须比能力更清楚。

**屏幕文字**

`Goal / Inputs / Workflow / Permissions`

`Verification / Stop Conditions / Output`

---

### 06｜文件，就是 AI 的工作台（2:08–2:31）

**画面**

文件树展开：`artifacts/`、`loop-contract.md`、`LOGS.md`。资料流入 artifacts；规则锁进 contract；每轮动作追加进日志时间线。

**旁白**

稳定的 Loop，也不能只依赖聊天窗口里的记忆。

`artifacts` 是资料区，存放文档、信号、任务和中间产物；`loop-contract.md` 是规则；`LOGS.md` 是运行记录。

每一次观察、判断、动作、失败和下一步，都被写下来。工程师早上接手时，看到的不是黑箱结果，而是一条可以追踪、复盘和继续执行的链路。

**屏幕文字**

`artifacts = 资料`

`contract = 规则`

`LOGS = 过程`

---

### 07｜真实系统不是一个大 Loop（2:31–3:13）

**画面**

横向流水线展开：`sync → feedback → task → coding → verify → followup`。每个节点由独立 worker 执行。DeepSeek 标记在归类、判断、验证和文案节点；Cursor 标记在 coding 节点。

**旁白**

真实系统通常不是让一个超级 Agent 从头干到尾，而是把复杂工作拆成多个小 Loop。

`sync` 同步反馈，`feedback` 提炼信号，`task` 判断是否生成任务，`coding` 执行修复，`verify` 对照验收标准验证，`followup` 生成回访草稿。

不同模型和工具只负责自己擅长的环节。它们通过文件、状态和明确的输入输出协作，而不是共享一团模糊的上下文。

**屏幕文字**

`sync → feedback → task → coding → verify → followup`

---

### 08｜两个门禁，比自动化更重要（3:13–3:39）

**画面**

流程在 coding 前停住，任务状态从 `proposed` 切换为 `approved` 后才放行。随后 followup 生成邮件草稿，发送按钮保持锁定。

**旁白**

这条流水线里，有两个关键门禁。

第一，任务只有被人工批准，AI 才能开始改代码。第二，AI 可以生成用户回访草稿，但绝不自动发送。

因为代码修改、生产部署和用户沟通，承担的风险不同。好的 Loop 不是追求无人化，而是把人放在真正需要判断和负责的位置。

**屏幕文字**

`Gate 1: task.status === approved`

`Gate 2: Draft only. Never auto-send.`

---

### 09｜它不只会修 Bug（3:39–4:02）

**画面**

同一个 Loop 骨架切换成三个场景：客服工单聚类、SEO 关键词增长曲线、产品实验 PR。底层循环保持不变。

**旁白**

Loop Engineer 也不只用于修 Bug。

客服可以定时读取工单，把高频问题沉淀成产品信号。SEO 可以每天发现机会、生成页面，再持续监控表现。产品增长可以读取行为数据、提出实验，生成一个等待审核的小 PR。

场景不同，结构相同：观察、执行、验证、记录，再进入下一轮。

**屏幕文字**

`Support / SEO / Growth / Engineering`

---

### 10｜工程师的角色正在变化（4:02–4:28）

**画面**

镜头从自动运行的细节操作逐渐拉远。工程师站在控制台前，不再逐条敲命令，而是在配置目标、权限、验证和门禁。所有 Loop 在一张系统图中稳定运行。

**旁白**

Loop Engineer 改变的，不只是写代码的方式，也是工程工作的组织方式。

人的角色，正在从亲自盯住每一步，转向设计目标、准备上下文、配置工具、设置验证、控制权限和审核结果。

真正值得自动化的，不只是“生成一段代码”，而是那些需要反复观察、尝试、反馈和改进的完整任务。

---

### 11｜结尾：从会说，到会做（4:28–4:45）

**画面**

回到凌晨三点。测试全部变绿，PR 和验证报告已经生成；工程师仍在睡觉。画面定格在闭环图，随后收束成一句话。

**旁白**

Loop Engineer 的核心，就是让 AI 在一个有目标、有上下文、有工具、有验证、有边界的循环里持续工作。

从 Prompt 到 Loop，是 AI 从“会说”走向“会做”的关键一步。

**屏幕文字**

`From Prompt to Loop.`

`让 AI 不只回答问题，而是推进结果。`

**音效**

报警声消失，留下轻微的系统完成提示音。

---

## 剪辑与包装建议

1. 前 20 秒保持高密度，用报警、日志和测试失败快速建立冲突。
2. 第 04–08 段使用同一套节点语言，让观众始终知道自己位于闭环的哪一层。
3. 技术名词只保留在屏幕文字中，旁白优先说人话，降低理解门槛。
4. `Loop Contract`、`Verify`、两个“人工门禁”是全片三个重点，应给予明显停顿和视觉强化。
5. 结尾回扣“凌晨三点”，形成完整叙事闭环。

## 可选标题

- AI 编程的下一步，不是更强的 Prompt，而是 Loop
- Loop Engineer：让 AI 真正推进工程任务
- 凌晨三点，先让 AI 去修线上 Bug
- 从 Prompt 到 Loop：AI 如何从“会说”走向“会做”

## 封面文案

主标题：`让 AI 自己跑完工程闭环`

副标题：`Loop Engineer 到底是什么？`
