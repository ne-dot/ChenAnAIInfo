# Loop Engineer 视频分段方案 V3

## 全片统一规则

- 内容依据：《Loop Engineer：旁白 V3》与定稿文章。
- 旁白速度：1.1 倍速。
- 预计总时长：约 6 分钟。
- 形式：无真人出镜的科技解释视频，旁白与字幕在剪辑软件中后期添加。
- 画面母版保持统一：多色科技极光背景 → 顶部标题区 → 下方动画区 → 底部约 125px 字幕安全区。
- 旁白承担完整叙事，画面只负责展示当下正在说的内容。
- 不使用“普通 AI”“替代人”“更先进”等比较或评判性表达。
- 不提前展示后续段落的信息。一个画面只服务当前旁白。
- 每段单独生成，开头和结尾保留约 0.5 秒稳定画面，方便剪辑衔接。

---

## 第 1 段｜Loop Engineer 修复阻塞性 Bug

**对应旁白**

《Loop Engineer：自己写的旁白》第一段全文。

**预计时长**

约 55-65 秒。

**顶部标题**

`Loop Engineer 修复阻塞性 Bug`

**动画设计**

拆成 3 个连续动画块，前两块重新做场景铺垫，第三块沿用原来的 DEMO 展示。

**1｜凌晨三点，线上出了阻塞性 Bug**

- 画面从黑场进入 `03:00 AM`，背景保持安静的深色值班感。
- 中央出现生产系统状态面板，状态从 `OK` 跳到 `BLOCKING BUG`。
- 三个信号按旁白顺序依次亮起：`线上阻塞性 Bug`、`客服反馈`、`监控报警`。
- 客服反馈可以表现为一条用户消息卡片滑入；监控报警表现为红色脉冲和告警曲线抬升。
- 本块只建立事故现场，不出现工程师操作，也不提前展示 AI。

**2｜过去的人工排查链路**

- 画面切到工程师被叫醒的极简场景：手机告警亮屏、电脑打开、终端启动。
- 下方形成一条手动流程线，按旁白逐步点亮：
  `看日志` -> `复现问题` -> `定位代码` -> `修改` -> `跑测试` -> `提 PR` -> `合并上线`
- 每一步都表现为需要人工切换窗口和等待结果，节奏略慢，但不做夸张焦虑表达。
- 结尾流程线被压缩成一串待办，作为过渡交给下一块。

**3｜DEMO：交给 Loop Engineer 处理**

- 使用原来的真实 DEMO 展示作为主画面，左侧或中间保留录屏主体，右侧固定显示“Loop Engineer 正在推进”的步骤栏。
- 右侧步骤文案与旁白一致，按真实进展逐个高亮：
  1. `查看用户反馈`
  2. `读取错误信息`
  3. `查看服务器日志`
  4. `定位相关代码`
  5. `尝试修改一版`
  6. `运行测试`
  7. `测试通过：整理修复记录`
  8. `生成 PR，等待工程师审核`
- 如果 DEMO 中出现测试失败或越界情况，右侧切到另一条停止路径：
  `测试未通过 / 超出约定范围` -> `停止推进` -> `整理已查信息` -> `列出尝试修改` -> `说明失败原因` -> `交给工程师接手`
- 结尾画面切到第二天公司视角：工程师打开工作记录，看到的是一份结构化推进记录，而不是零散报错。
- 结构化记录建议显示 4 个区块：`问题摘要`、`证据与日志`、`尝试过的修改`、`当前状态 / 下一步`。

**真实素材**

- 用户提供的修 Bug DEMO 录屏。
- 未提供录屏前保留空白替换区域。

**本段不出现**

- Prompt 与 Loop 对比。
- 六大组成部分。
- 多 Loop 流水线。

**衔接**

从 DEMO 的连续操作拉远，进入这套工作方式的名称和变化。

---

## 第 2 段｜从一步步沟通，到连续推进

**对应旁白**

《旁白 V3》第二段全文。

**预计时长**

约 35 秒。

**顶部标题**

`从一步步沟通，到连续推进`

**动画设计**

1. 开场展示一条对话时间线：人发出一步指令，AI 返回一次结果。
2. 后续动作需要人再次确认，形成“看结果 → 再告诉下一步”的节奏。
3. 时间线逐渐连接成一条连续执行路径。
4. AI 自动读取当前状态，把执行结果带入下一步。
5. 最终形成一个简洁循环：状态 → 动作 → 反馈 → 下一步。
6. 结尾出现概念名称：`Loop Engineer`。

**表达重点**

只描述协作方式从“逐步沟通”扩展到“边界内连续推进”，不评价两种方式的高低。

**本段不出现**

- “普通 AI 编程”。
- 左右优劣对比。
- “流程停止”等负面标签。

**衔接**

镜头靠近刚形成的循环，下一段拆解循环内部怎样运行。

---

## 第 3 段｜一次 Loop 怎样运行

**对应旁白**

《旁白 V3》第三段全文。

**预计时长**

约 35 秒。

**顶部标题**

`一次 Loop 怎样运行`

**动画设计**

1. 使用单一闭环舞台，不做多个卡片页面。
2. 节点按旁白顺序出现：观察状态 → 读取上下文 → 选择动作 → 调用工具 → 获得结果 → 验证。
3. 验证通过，光点流向“可交付结果”。
4. 验证未通过，失败信息沿回路返回“上下文”。
5. 第二轮运行时只更新关键状态，让观众看见反馈如何进入下一轮。
6. 结尾把闭环放回真实软件工程节奏：看现象、找原因、修改、再验证。

**表达重点**

这段只解释一次循环的运行机制，不介绍系统组件和权限。

**衔接**

循环保持在画面中央，下一段从循环中拆出支撑它运行的组成部分。

---

## 第 4 段｜支撑 Loop 运行的组成部分

**对应旁白**

《旁白 V3》第四段全文。

**预计时长**

约 50 秒。

**顶部标题**

`支撑 Loop 运行的组成部分`

**动画设计**

采用一个不断组装的系统结构，而不是六张并列卡片。

1. `Trigger` 从监控、定时任务、工单或 issue 中发出启动信号。
2. 信号进入 `Context`，代码、日志、文档、反馈和测试结果汇聚到上下文容器。
3. `Tools` 接到动作，展示搜索、读取、修改、运行和生成 PR。
4. 执行结果进入 `Verify`，展示测试、类型检查、接口检查和截图验证。
5. 每次动作同步写入 `Logs`。
6. 验证完成后形成 `Output`。
7. 结尾展示这些部分如何连接成一条可持续运行的路径。

**表达重点**

各组成部分随着旁白逐步加入，避免一次把所有名词堆在屏幕上。

**衔接**

系统外侧出现一层尚未命名的边界，进入 Loop Contract。

---

## 第 5 段｜Loop Contract 提供边界

**对应旁白**

《旁白 V3》第五段全文。

**预计时长**

约 45 秒。

**顶部标题**

`Loop Contract：持续运行中的任务边界`

**动画设计**

1. 延续上一段的系统结构，在最外层形成 `Loop Contract` 边界。
2. 合同内容依次写入：目标、输入、流程、权限、验证、停止条件、交付。
3. 使用修 Bug 场景实例化权限：读取日志、修改指定模块、运行测试、生成 PR。
4. 生产部署、敏感信息、范围外代码停留在边界之外。
5. 连续失败、信息不足和范围越界分别触发停止状态。
6. 当前上下文、修改记录和失败原因被整理成人工接手包。

**表达重点**

边界表现为可观察的系统规则，不使用警告式或教育式表达。

**衔接**

合同收束成一个真实文件 `loop-contract.md`，进入文件工作台。

---

## 第 6 段｜文件是 AI 的工作台

**对应旁白**

《旁白 V3》第六段全文。

**预计时长**

约 28 秒。

**顶部标题**

`文件是 AI 的工作台`

**动画设计**

1. 展示简洁文件树，而不是模拟完整代码编辑器。
2. `artifacts/` 接收文档、信号、任务和中间产物。
3. `loop-contract.md` 固定在规则层。
4. `LOGS.md` 随运行轮次不断追加观察、动作、结果和下一步。
5. 最后切到工程师接手视角：打开文件即可看到任务进度和完整记录。

**表达重点**

突出“信息在多轮执行中保持稳定”和“任务可以被接手”。

**衔接**

文件树中的一个任务展开成多条独立工作流。

---

## 第 7 段｜多个小 Loop 协作完成任务

**对应旁白**

《旁白 V3》第七段全文。

**预计时长**

约 60 秒。

**顶部标题**

`多个小 Loop 协作完成一次修复`

**动画设计**

1. 横向流水线逐步建立：`sync → feedback → task → coding → verify → followup`。
2. `sync` 汇入工单、反馈和异常。
3. `feedback` 把零散信息整理成信号。
4. `task` 生成待处理任务。
5. 第一处人工节点出现：批准后才进入 `coding`。
6. `coding` 产生代码修改，`verify` 生成验证报告。
7. `followup` 生成回访草稿。
8. 第二处人工节点出现：草稿停在发送之前。
9. 结尾切换到早晨的控制面板，完整展示反馈、任务、代码、验证和待确认动作。

**表达重点**

每个 Loop 只处理一段职责，通过文件和状态协作。人工节点是流程的一部分，不用“阻止 AI”或“防止失控”的措辞。

**衔接**

修 Bug 流水线缩小，旁边出现客服、SEO 和产品增长的同构流程。

---

## 第 8 段｜Loop 可以出现在哪里

**对应旁白**

《旁白 V3》第八段全文。

**预计时长**

约 35 秒。

**顶部标题**

`Loop 可以出现在哪里`

**动画设计**

1. 同一个循环骨架保持不动，内容依次切换为三个场景。
2. 客服支持：读取工单 → 整理高频问题 → 沉淀产品信号。
3. SEO：分析数据 → 发现关键词 → 生成草稿 → 观察表现。
4. 产品增长：读取行为 → 整理实验 → 生成待审核改动。
5. 三个场景最终叠合出共同节奏：观察 → 动作 → 反馈 → 下一步。

**表达重点**

只展示可能的工作组织方式，不承诺自动化效果，也不鼓励观众立即采用。

**衔接**

多个应用场景逐渐淡出，只留下统一的 Loop 结构。

---

## 第 9 段｜AI 开始进入任务推进过程

**对应旁白**

《旁白 V3》第九段全文。

**预计时长**

约 35 秒。

**顶部标题**

`AI 开始进入任务推进过程`

**动画设计**

1. 目标、上下文、工具、验证、权限和记录依次回到同一套结构中。
2. 工程师视角从逐步发送指令，转向准备上下文、设置边界和查看关键节点。
3. 回扣凌晨三点的 Bug，展示它只是众多循环中的一个实例。
4. 镜头拉远，更多需要反复查看、尝试和验证的任务连接到同一结构。
5. 结尾停在文章定稿观点：`AI 开始进入任务持续推进的过程`。

**结尾方式**

不使用口号式 CTA，不要求点赞、关注或尝试，只自然结束分享。

---

## 分段与旁白对应总表

| 视频段落 | V3 旁白范围 | 预计时长 | 核心画面 |
|---|---|---:|---|
| 01 | 第一段 | 60 秒 | 凌晨事故、人工排查链路、修 Bug DEMO |
| 02 | 第二段 | 35 秒 | 逐步沟通连接成连续推进 |
| 03 | 第三段 | 35 秒 | 一次 Loop 的运行机制 |
| 04 | 第四段 | 50 秒 | Trigger 到 Output 的组装过程 |
| 05 | 第五段 | 45 秒 | Loop Contract 边界 |
| 06 | 第六段 | 28 秒 | artifacts、contract、logs |
| 07 | 第七段 | 60 秒 | 多 Loop 修复流水线与人工节点 |
| 08 | 第八段 | 35 秒 | 客服、SEO、产品增长 |
| 09 | 第九段 | 35 秒 | 变化总结与凌晨三点回扣 |

预计总时长：约 6 分 23 秒。
