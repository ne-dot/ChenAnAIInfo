# Loop Engineer 视频旁白 V3

> 语气：像把一个刚做完的 Demo 分享给朋友，不说教，不反问，不制造优劣对立。  
> 语速：1.1 倍速。  
> 结构：DEMO 场景 → 概念 → 运行结构 → 多 Loop 案例 → 应用与变化。

## 第一段｜从一个修 Bug DEMO 开始

这是我最近做的一个 Loop Engineer Demo。

我把场景放在凌晨三点。线上出了一个阻塞性 Bug，用户卡住了，客服收到了反馈，监控也开始报警。

以前碰到这种情况，工程师大概率要爬起来，打开电脑，看日志，复现问题，定位代码，改完再一遍遍跑测试。

在这个 Demo 里，我先把这些排查动作交给 AI。

它会先读错误信息，再看日志，找到相关代码，尝试改一版，然后跑测试。

如果测试没过，它就继续看失败原因，调整代码，再跑一次。

这个过程会一直往前走，直到测试通过，或者信息不够了，需要人接手。

## 第二段｜从一步步沟通，到连续推进

之前我们和 AI 协作，很多时候还是一来一回地聊。

先让它看报错，再让它找代码。它给出结果以后，人再告诉它下一步干什么。

但有些边界比较清楚的任务，其实已经可以把这一串动作连起来。

AI 先读取当前状态，理解已有信息，然后自己选下一步动作。执行完以后，它再看反馈，继续调整。

它不只是回你一句话，而是围绕一个结果，一步一步往前推进。

我理解的 Loop Engineer，就是把 AI 放进这种执行和反馈的循环里。

## 第三段｜一个 Loop 怎样运行

把刚才这个修复过程拆开，其实路径很简单。

先看现在发生了什么，再读完成任务需要的上下文。接着选择动作，调用工具，拿到执行结果，然后做验证。

验证通过，这一轮就可以交付。验证没通过，失败信息就变成下一轮的新上下文。

很多工程工作本来就是这么做的：看现象，找原因，改一点，跑一下，再看新的结果。

Loop Engineer 做的事，就是把 AI 放进这条已经存在的工程循环里。

## 第四段｜Loop 的几个组成部分

一个 Loop 真要跑起来，通常少不了几个东西。

先是 Trigger，也就是启动信号。它可以来自监控报警、定时任务、客服工单，也可以是一条被标记为 Bug 的 issue。

然后是 Context。代码、日志、产品文档、用户反馈、历史提交、测试结果，都会影响 AI 下一步怎么判断。

再往下是 Tools。搜索代码、读取文件、修改内容、运行命令、生成 PR，这些都是它可以调用的动作。

动作做完以后，还要有 Verify。单元测试、类型检查、接口检查、页面截图，或者一段业务验证脚本，都可以给它反馈。

最后，整个过程会写进 Logs，也会形成一个可以检查的 Output。

这些东西连起来，一次任务才不只是“聊一下”，而是真的可以持续往前跑。

## 第五段｜Loop Contract 提供边界

Loop 能持续跑，还需要一层比较稳定的边界，我这里叫它 Loop Contract。

它有点像一份任务合同，提前写清楚这次要做什么，可以读哪些输入，可以用哪些工具，怎么验证，什么时候停，最后交付什么。

比如在修 Bug 这个场景里，AI 可以读日志，可以改指定模块，可以跑测试，也可以生成 PR。

但生产部署、敏感信息，还有范围外的代码，就留在边界外面。

如果测试连续失败，信息不够，或者修改范围超出了约定，Loop 就停下来，把当前状态交给人。

这样它虽然在连续执行，但整个过程还是看得见、查得到，也随时可以接手。

## 第六段｜文件是 AI 的工作台

为了让这些信息在多轮执行里不丢，我在 Demo 里放了一套文件结构。

`artifacts` 保存文档、外部信号、当前任务和中间产物。

`loop-contract.md` 保存任务规则。

`LOGS.md` 记录每一轮看到了什么、做了什么、拿到了什么结果，下一步准备怎么走。

工程师接手的时候，直接看这些文件，就能知道事情走到哪一步了，不用重新猜。

## 第七段｜真实任务由多个小 Loop 协作

在完整的修 Bug 流程里，我没有让一个 Agent 从头干到尾。

我把它拆成了几个职责比较清楚的小 Loop。

`sync` 负责同步客服反馈和异常信息。

`feedback` 负责归类反馈，提炼成问题信号。

`task` 根据这些信号生成一条待处理任务。

任务经过人工批准以后，`coding` 才会进入代码仓库做修复。

修复完成后，`verify` 对照验收标准生成验证报告。

最后，`followup` 根据结果生成用户回访草稿。

这里我特意保留了两个人工节点。

一个在代码修改之前，任务要先被批准。

另一个在用户沟通之前，系统只生成草稿，不会自动发出去。

工程师早上打开系统，看到的就是一条完整记录：同步了哪些反馈，形成了什么任务，改了哪些代码，验证结果怎么样，还有哪些动作等人确认。

## 第八段｜Loop 可以出现在哪里

修 Bug 只是一个比较容易看懂的场景。

类似的循环也可以放在客服支持里：定时读取新工单，整理高频问题，再沉淀成产品信号。

也可以放在 SEO 工作里：分析数据，发现关键词机会，生成页面草稿，再持续看发布后的表现。

在产品增长里，它可以读取用户行为，整理实验方向，生成一个等待审核的小改动。

这些任务表面上不一样，但底层都有类似的节奏：持续观察，执行动作，看反馈，再决定下一步。

## 第九段｜结尾

做完这个 Demo，我最大的感受是，Loop Engineer 不是简单让 AI 多做几步。

它更像是把目标、上下文、工具、验证、权限和运行记录，放进同一套工作结构里。

人的工作也会从一条条发指令，慢慢转向准备上下文、设计边界、设置验证，然后在关键节点做判断。

半夜三点的线上 Bug，只是这个 Demo 里最容易看见的一次循环。

更多需要反复查看、尝试和验证的工作，也可以用类似的方式重新组织。

AI 不只是出现在一次对话里，它也开始进入任务持续推进的过程。
