# 开场白

最近 AI 编程圈里有个词开始火了，叫 Loop Engineering。是 Anthropic Claude Code 的负责人 Boris Cherny，和 OpenClaw 的创作者 Peter Steinberger，在 2026 年 6 月先后提出的，他们说明了一个变化：以后和 AI 协作，不只是写 Prompt，而是要设计一套循环系统，让 Agent 持续被触发、持续产出、持续积累状态，并且让不同 Agent 之间互相喂养。


# 第一段｜从一个修 Bug DEMO 开始

我根据Loop Engineer的思路做的一个Demo，只为展示其工作方式，并未到达生产级验证。

假如凌晨三点。线上出了一个阻塞性 Bug，客服收到了反馈，监控也开始报警。

以前碰到这种情况，工程师大概率要爬起来，然后打开电脑，看日志，复现问题，定位代码，改完再一遍遍跑测试，提PR，合并上线。

而现在我们把这些排查动作交给Loop Engineer来处理。在我的DEMO中实现了以下几个loop来修复线上问题

第1步，sync，同步客服工单、用户反馈和监控异常。

第2步，feedback，把零散反馈归类，提炼成明确的问题信号。

第3步，task，判断信号是否达到处理阈值，并生成一条待批准任务。

第4步，coding，进入代码仓库定位并修复问题。

第5步，verify，对照验收标准运行测试，生成验证报告。

第6步，followup，根据修复结果生成用户回访草稿。

工程师第2天醒来可以查看Loop Engineer的工作日志。

这条流水线中，有两个门禁尤其重要。

第一个门禁在 coding 之前。只有任务状态从 proposed 变成 approved，AI 才能修改代码。

第二个门禁在 followup 之后。AI 可以生成回访草稿，但绝不自动发送。

保留这两个人工节点，是因为发现问题、修改代码、部署生产和联系用户，承担的风险完全不同。好的 Loop 不是删除所有人，而是把人放在真正需要判断、授权和负责的位置。


# 第二段 | Loop Engineer到是什么？

Loop Engineer 可以理解成 agent 外层的运行系统。
它负责触发 agent runtime，也负责记录状态、日志和上下文。
这样 agent 每次被唤醒时，都知道发生了什么、之前做过什么、下一步该做什么。
触发信号可以来自很多地方：cron job、webhook、另一个 agent、support ticket、服务器 incident，或者数据指标异常。
每次被唤醒后，agent 会调查问题、执行动作，并产出 backlog、idea 或下一步建议。
主 agent 再根据这些结果排序优先级，必要时把任务分配给其他 agent。
所以Loop Engineer 就是让 agent 从一次性调用，变成一个持续运转的工作循环。


# 第三段 ｜ Loop Engineer 的核心结构

再来看看Loop Engineer 的核心结构，通常由几个核心部分组成：Trigger、Context、Tools、Verify，以及贯穿整个系统的 Loop Contract。

Trigger 是触发器，决定 loop 什么时候开始。它可以是定时任务、webhook、监控报警，也可以是用户反馈或线上错误日志。

Context 是上下文，决定 AI 能不能判断问题。AI 需要读取代码、日志、文档、用户反馈、测试结果和最近提交记录。没有上下文，AI 只能猜；有上下文，AI 才能定位真正原因。

Tools 是工具能力，决定 AI 能不能执行动作。比如搜索代码、读取文件、修改文件、运行测试、查看 diff、创建分支、生成验证报告。只有具备工具能力，AI 才不只是顾问，而是能推进任务的工程代理。

Verify 是验证机制，决定 AI 的动作是否有效。修 Bug 后测试是否通过，接口返回是否正确，页面是否正常，验收标准是否满足，都属于验证。没有验证，就没有真正的 loop。

但这里最关键的是 Loop Contract。

Loop Contract 不是流程里的一个普通步骤，而是整个 loop 的约束层。它规定 AI 要完成什么目标，能读取什么，能修改什么，不能越过哪些边界，怎么验证，什么时候停止，以及最后交付什么。

回到我的 Demo 里，每一个 Loop，都有一份必须遵守的合同。合同会写清：这个 Loop 扮演什么角色、本轮目标是什么、由什么触发、开始前要读哪些输入、最终写出什么产出。同时明确能做什么、不能做什么，守住合入、发信、结案这些硬边界。

合同里还必须有 Verify——也就是怎样判断这一轮算通过。没有可执行的验证，就没有真正的 Loop。还要写清什么时候停止，以及如何记日志：成功、跳过、失败都会写入 log；失败时再按约定发出失败事件。

Loop Contract 的价值，是让 AI 的自动化有边界。它不是“你自己看着办”，而是“在这份合同内循环推进”。

整条链路里，Sync 负责收录用户反馈，线上问题；Feedback 把反馈归类成信号；Task 为成熟信号生成待批准的任务；Coding 在批准后修复代码；Verify 对照验收标准独立验收；Followup 只写回访草稿，发送和结案留给人工。

把复杂工程任务拆成多个有边界的小 loop。每个 loop 的 contract 保证职责清晰、权限可控、结果可验证。

Demo中定义的边界：不能自动合入 main，不能自动部署生产，不能自动给用户发信，不能把 feature 或 question 类反馈直接变成 coding 任务，也不能绕过人工门禁。但这仅限于我Demo的规定。宝子们可以根据自己的情况制定好边界, 例如P0级线上问题，反馈过多可以尝试让loop Engineer部署修复，这样就和前面那个凌晨3点例子闭环了。门禁和边界完全由自己的阈值来确定。


# 第四段 ｜ 文件结构：AI 的工作台

Loop Engineer 需要一个可接手、可审计的工作台。

这个工作台最小可以看成三块。

第一块是 artifacts/，资料区，也就是装过程文件的文件夹。Loop 每走一步，都会在这里记录信息
/feedback： sync worker获取用户反馈线上问题；
/signals： 从上面feedback信息总结成信号；
/tasks 根据信号总结成任务；
/verifications 放验证报告；
/followups 放回访草稿。
其中反馈和回访含用户原文，通常不提交；信号、任务、验证报告可提交，方便审计。

第二块是合同，前面的内容已经介绍过了。这里就不过多赘述

第三块是 log.md，运行记录。每一轮写清触发了什么、做了什么判断、成功还是失败、卡在哪里。第二天打开项目，不用猜 AI 半夜干了啥，直接看日志就能复盘。

所以这三块各解一个问题：artifacts/ 沉淀上下文，合同限定边界，日志负责追踪。有了它们，Loop Engineer 才从「一次 AI 输出」，变成可持续运行的机制——有触发、有上下文、有工具、有验证、有边界、有沉淀。AI 不只是回答问题，而是在受约束、可追踪、可验证的系统里，持续推进工程任务。

# 第五段 ｜ Loop Engineer 也不只用于修 Bug。

Loop Engineer 也不只用于修 Bug

客服可以定时读取工单，把高频问题沉淀成产品信号。SEO 可以每天发现机会、生成页面，再持续监控表现。产品增长可以读取行为数据、提出实验，生成一个等待审核的小 PR。

场景不同，结构相同：观察、执行、验证、记录，再进入下一轮。

## 第六段 ｜ 工程师的角色正在变化

Loop Engineer 改变的，不只是写代码的方式，也是工程工作的组织方式。

人的角色，正在从亲自盯住每一步，转向设计目标、准备上下文、配置工具、设置验证、控制权限和审核结果。

真正值得自动化的，不只是“生成一段代码”，而是那些需要反复观察、尝试、反馈和改进的完整任务。

# 结尾

Loop Engineer 就是让 AI 在一个有目标、有上下文、有工具、有验证、有边界的循环里持续工作。

从 Prompt 到 Loop，是 AI 从“会说”走向“会做”的关键一步。